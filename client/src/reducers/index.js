const initialState = {
  users: [],
  error: '',
  isLoggedIn: false,
  isLoading: false,
}

export const reducer = (state = initialState, action) => {
  switch(action) {
    default :
      return state
  }
}